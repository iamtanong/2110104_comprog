#include<bits/stdc++.h>
#include<math.h>
using namespace std;

int main() {

    double u, zeta, t;
    cin >> u >> zeta >> t;

    // double usinzeta = u* sin((zeta * M_PI)/180);

    // double h = (t- (usinzeta/10))*(usinzeta + (5*(t - (0.3*usinzeta))));
    // double r = u*cos((zeta * M_PI)/180)*t;

    // cout << round(h*10)/10 << " " << round(r*10)/10;

    return 0;
}